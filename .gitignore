# dbt artifacts
target/
dbt_packages/
logs/

# Secrets / local config
.env
profiles.yml

# Ignore JSON everywhere (e.g. service-account keyfiles), but keep exported data
*.json
!data/
!data/*.json

# Keep dashboard manifests and bundled data (still ignores node_modules/dist via dashboard/.gitignore)
!dashboard/package.json
!dashboard/package-lock.json
!dashboard/public/data/
!dashboard/public/data/*.json
